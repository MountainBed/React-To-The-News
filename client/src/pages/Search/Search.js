import React from "react";
import Results from "../../components/Results";

const Search = () => (
    <div>
        <div>Search is loading!</div>
        <Results />
    </div>
);

export default Search;